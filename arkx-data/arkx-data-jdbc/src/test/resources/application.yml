spring:
  datasource:
    url: jdbc:h2:mem:productdb
    driver-class-name: org.h2.Driver  # ? driverClassName ??? kebab-case???????
    username: sa
    password: ""  # ?????????????????

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql  # ???? schema ???????- classpath:schema1.sql - classpath:schema2.sql
      data-locations: classpath:data.sql      # ????? data ?????

  sqltoy:
    # ????????sql?????(?????????????????),?????????????????
    sqlResourcesDir: classpath:/sql
    # ???????
    #translateConfig: classpath:sqltoy-translate.xml
    # debug???????sql
    debug: true
    # ??????:createBy createTime updateBy updateTime ??????(???)??(????)
    #unifyFieldsHandler: com.sqltoy.plugins.SqlToyUnifyFieldsHandler
    # sql???????????????,???????sql(????:??30?)
    printSqlTimeoutMillis: 3000000