<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rapidark.cloud.base.server.mapper.BaseRoleUserMapper">
    <resultMap id="BaseResultMap" type="com.rapidark.cloud.base.client.model.entity.SysUserRole">
        <!--
          WARNING - @mbg.generated
        -->
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
    </resultMap>

    <select id="selectRoleUserList" resultMap="com.rapidark.cloud.base.server.mapper.BaseRoleMapper.BaseResultMap">
        SELECT
        sys_user_role.user_id,
        base_role.role_id,
        base_role.role_code,
        base_role.role_name,
        base_role.role_desc,
        base_role.status,
        base_role.create_time,
        base_role.update_time
        FROM sys_user_role
        INNER JOIN base_role ON sys_user_role.role_id = base_role.role_id
        WHERE user_id = #{userId} AND status = 1
    </select>


    <select id="selectRoleUserIdList" resultType="java.lang.Long">
        SELECT
        base_role.role_id
        FROM sys_user_role
        INNER JOIN base_role ON sys_user_role.role_id = base_role.role_id
        WHERE user_id = #{userId} AND status = 1
    </select>
</mapper>
